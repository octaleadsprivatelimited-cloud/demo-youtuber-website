'use client';import{useEffect,useState}from'react';import{listPublicRecords,type SiteRecord}from'@/services/site-data';
export function CmsHomepageModules(){const[data,setData]=useState<Record<string,SiteRecord[]>>({});useEffect(()=>{const load=()=>Promise.all(['banners','advertisements','tractors','articles','videos','brands'].map(async name=>[name,await listPublicRecords(name,8)]as const)).then(rows=>setData(Object.fromEntries(rows)));load();window.addEventListener('rj-demo-data',load);return()=>window.removeEventListener('rj-demo-data',load);},[]);const banner=data.banners?.[0];const ad=data.advertisements?.find(x=>String(x.placement??'').toLowerCase().includes('homepage'));const groups=[['tractors','Featured from the CMS','/tractors'],['articles','Latest published articles','/articles'],['videos','Latest published videos','/videos']]as const;if(!Object.values(data).some(x=>x.length))return null;return <section className="cms-home">{banner&&<a className="cms-banner" href={String(banner.ctaUrl??'#')} style={banner.image?{backgroundImage:`linear-gradient(90deg,rgba(7,20,11,.9),rgba(7,20,11,.25)),url(${banner.image})`}:undefined}><p>FEATURED</p><h2>{String(banner.title)}</h2><span>{String(banner.subtitle??'')}</span><strong>{String(banner.ctaLabel??'Explore now')} →</strong></a>}{groups.map(([key,title,href])=>data[key]?.length?<div className="cms-group" key={key}><header><h2>{title}</h2><a href={href}>View all →</a></header><div>{data[key].slice(0,4).map(item=><a key={item.id} href={key==='tractors'?`/tractor/${String(item.brand??item.brandName??'brand').toLowerCase().replaceAll(' ','-')}/${item.slug}`:key==='articles'?`/articles/${item.slug}`:`/videos/${item.slug}`}><span style={(item.image||item.thumbnail)?{backgroundImage:`url(${String(item.image??item.thumbnail)})`}:undefined}/><small>{String(item.brand??item.category??key.slice(0,-1))}</small><h3>{String(item.title??item.model??item.name)}</h3></a>)}</div></div>:null)}{ad&&<a className="cms-ad" href={String(ad.destinationUrl??'#')}><small>ADVERTISEMENT</small>{ad.image?<img src={String(ad.image)} alt={String(ad.title??'Advertisement')}/>:<strong>{String(ad.title)}</strong>}</a>}</section>}
