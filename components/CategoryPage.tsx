'use client';
import {useEffect,useState} from 'react';import {PublicShell} from './SiteChrome';import {SetupNotice} from './SetupNotice';import {listArticles,type Article} from '@/services/media';import {isFirebaseConfigured} from '@/lib/firebase/client';import {titleFromSlug} from '@/utils/seo';
export function CategoryPage({slug}:{slug:string}){const [items,setItems]=useState<Article[]>([]);const [loading,setLoading]=useState(isFirebaseConfigured);useEffect(()=>{if(!isFirebaseConfigured)return;listArticles(undefined,slug).then(setItems).finally(()=>setLoading(false));},[slug]);return <PublicShell><main><section className="page-hero"><p>AGRICULTURE TOPIC</p><h1>{titleFromSlug(slug)}</h1><span>Published news, explainers and practical guides in this category.</span></section>{!isFirebaseConfigured?<SetupNotice/>:<section className="article-list">{loading?<div className="detail-loading">Loading stories…</div>:!items.length?<div className="empty-state"><h3>No published stories in this category.</h3><a href="/articles">Browse all articles →</a></div>:<div className="article-grid">{items.map(item=><article key={item.id}><div style={item.coverImage?{backgroundImage:`url(${item.coverImage})`}:undefined}/><section><p>{item.categoryName}</p><h2>{item.title}</h2><span>{item.excerpt}</span><a href={`/articles/${item.slug}`}>Read story →</a></section></article>)}</div>}</section>}</main></PublicShell>}
