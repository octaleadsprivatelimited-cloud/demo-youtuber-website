'use client';
import{useEffect,useState}from'react';import type{AdminSection}from'@/config/admin-sections';import{listAdminRecords,removeAdminRecord,saveAdminRecord,uploadAdminImage,type AdminRecord}from'@/services/admin';
function RichEditor({value,onChange}:{value:string;onChange:(v:string)=>void}){const add=(before:string,after='')=>onChange(value+`\n${before}Text${after}`);return <div className="rich-editor"><div><button type="button" onClick={()=>add('## ')}>Heading</button><button type="button" onClick={()=>add('**','**')}>Bold</button><button type="button" onClick={()=>add('*','*')}>Italic</button><button type="button" onClick={()=>add('- ')}>List</button><button type="button" onClick={()=>add('> ')}>Quote</button><button type="button" onClick={()=>add('[Link](',')')}>Link</button><button type="button" onClick={()=>add('https://www.youtube.com/watch?v=')}>YouTube</button><button type="button" onClick={()=>add('| Column | Column |\n|---|---|\n| Value | Value |')}>Table</button></div><textarea value={value} onChange={e=>onChange(e.target.value)} placeholder="Write the article body…"/></div>}
export function AdminCrud({section}:{section:AdminSection}){const[items,setItems]=useState<AdminRecord[]>([]);const[editing,setEditing]=useState<AdminRecord|null>(null);const[form,setForm]=useState<Record<string,unknown>>({status:'draft'});const[busy,setBusy]=useState(false);const[error,setError]=useState('');async function load(){setBusy(true);setError('');try{setItems(await listAdminRecords(section.collection));}catch(e){setError(e instanceof Error?e.message:'Unable to load records.');}finally{setBusy(false);}}useEffect(()=>{load();},[section.collection]);function open(item?:AdminRecord){setEditing(item??null);setForm(item?{...item}:{status:'draft'});}async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);try{await saveAdminRecord(section.collection,editing?.id,form);setEditing(null);setForm({status:'draft'});await load();}catch(e){setError(e instanceof Error?e.message:'Unable to save.');setBusy(false);}}async function remove(item:AdminRecord){if(confirm(`Delete ${String(item.title??item.model??item.id)}?`)){await removeAdminRecord(section.collection,item.id);await load();}}return <><header className="admin-heading"><div><p>CONTENT MANAGEMENT</p><h1>{section.label}</h1><span>{section.description}</span></div><button onClick={()=>open()}>+ New record</button></header>{section.collection==='videos'&&<div className="admin-panel sync-panel"><strong>YouTube Data API sync</strong><span>Manual video management is active. Automatic sync runs through the secure Cloud Function when a YouTube API key is configured.</span><button onClick={()=>setError('Deploy the syncYouTubeVideos Cloud Function and configure YOUTUBE_API_KEY to enable automatic channel sync.')}>Sync YouTube videos</button></div>}{error&&<div className="admin-error">{error}</div>}<div className="admin-panel"><div className="admin-table-head"><strong>{items.length} records</strong><button onClick={load}>Refresh</button></div>{busy&&!editing?<div className="detail-loading">Loading…</div>:!items.length?<div className="empty-state"><h3>No records yet.</h3><p>Create the first record.</p></div>:<div className="crm-table-wrap"><table className="crm-table"><thead><tr><th>Name</th><th>Status</th><th>Identifier</th><th>Actions</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{String(item.title??item.model??item.name??item.email??'Untitled')}</strong></td><td>{String(item.status??'draft')}</td><td><small>{String(item.slug??item.path??item.id)}</small></td><td><button onClick={()=>open(item)}>Edit</button> <button className="danger-link" onClick={()=>remove(item)}>Delete</button></td></tr>)}</tbody></table></div>}</div>{(editing!==null||Object.keys(form).length>1)&&<div className="admin-modal"><form className="admin-form" onSubmit={submit}><button type="button" className="admin-close" onClick={()=>{setEditing(null);setForm({status:'draft'});}}>×</button><p>{editing?'EDIT RECORD':'NEW RECORD'}</p><h2>{section.label}</h2>{section.fields.map(field=><label key={field.key}>{field.label}{section.collection==='articles'&&field.key==='content'?<RichEditor value={String(form[field.key]??'')} onChange={value=>setForm({...form,[field.key]:value})}/>:field.type==='textarea'?<textarea required={field.required} value={String(form[field.key]??'')} onChange={e=>setForm({...form,[field.key]:e.target.value})}/>:field.type==='boolean'?<input type="checkbox" checked={Boolean(form[field.key])} onChange={e=>setForm({...form,[field.key]:e.target.checked})}/>:field.type==='status'?<select value={String(form[field.key]??'draft')} onChange={e=>setForm({...form,[field.key]:e.target.value})}><option value="draft">Draft</option><option value="review">Review</option><option value="pending">Pending</option><option value="published">Published</option><option value="approved">Approved</option><option value="rejected">Rejected</option><option value="archived">Archived</option></select>:field.type==='image'?<><input value={String(form[field.key]??'')} placeholder="Image URL" onChange={e=>setForm({...form,[field.key]:e.target.value})}/><input type="file" accept="image/*" onChange={async e=>{const file=e.target.files?.[0];if(file)setForm({...form,[field.key]:await uploadAdminImage(file,section.collection)});}}/></>:<input required={field.required} type={field.type==='number'?'number':'text'} value={String(form[field.key]??'')} onChange={e=>setForm({...form,[field.key]:field.type==='number'?Number(e.target.value):e.target.value})}/>}</label>)}<button className="cta-primary" disabled={busy}>{busy?'Saving…':'Save record'}</button></form></div>}</>}
