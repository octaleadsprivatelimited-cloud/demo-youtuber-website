'use client';
import {useEffect,useState} from 'react';
import type {AdminSection} from '@/config/admin-sections';
import {listAdminRecords,removeAdminRecord,saveAdminRecord,uploadAdminImage,type AdminRecord} from '@/services/admin';

export function AdminCrud({section}: {section:AdminSection}){const [items,setItems]=useState<AdminRecord[]>([]);const [editing,setEditing]=useState<AdminRecord|null>(null);const [form,setForm]=useState<Record<string,unknown>>({status:'draft'});const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function load(){setBusy(true);setError('');try{setItems(await listAdminRecords(section.collection));}catch(e){setError(e instanceof Error?e.message:'Unable to load records.');}finally{setBusy(false);}}useEffect(()=>{load();},[section.collection]);function open(item?:AdminRecord){setEditing(item??null);setForm(item?{...item}:{status:'draft'});}async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await saveAdminRecord(section.collection,editing?.id,form);setEditing(null);setForm({status:'draft'});await load();}catch(err){setError(err instanceof Error?err.message:'Unable to save.');setBusy(false);}}async function remove(item:AdminRecord){if(!confirm(`Delete ${String(item.title??item.model??item.id)}? This cannot be undone.`))return;await removeAdminRecord(section.collection,item.id);await load();}return <><header className="admin-heading"><div><p>CONTENT MANAGEMENT</p><h1>{section.label}</h1><span>{section.description}</span></div><button onClick={()=>open()}>+ New record</button></header>{error&&<div className="admin-error">{error}</div>}<div className="admin-panel"><div className="admin-table-head"><strong>{items.length} records</strong><button onClick={load}>Refresh</button></div>{busy&&!editing?<div className="detail-loading">Loading…</div>:!items.length?<div className="empty-state"><h3>No records yet.</h3><p>Create the first {section.label.toLowerCase()} record.</p></div>:<div className="crm-table-wrap"><table className="crm-table"><thead><tr><th>Name</th><th>Status</th><th>Identifier</th><th>Actions</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{String(item.title??item.model??item.tractorName??'Untitled')}</strong></td><td><span className="lead-status">{String(item.status??'draft')}</span></td><td><small>{String(item.slug??item.path??item.id)}</small></td><td><button onClick={()=>open(item)}>Edit</button> <button className="danger-link" onClick={()=>remove(item)}>Delete</button></td></tr>)}</tbody></table></div>}</div>{(editing!==null||Object.keys(form).length>1)&&<div className="admin-modal"><form className="admin-form" onSubmit={submit}><button type="button" className="admin-close" onClick={()=>{setEditing(null);setForm({status:'draft'});}}>×</button><p>{editing?'EDIT RECORD':'NEW RECORD'}</p><h2>{section.label}</h2>{section.fields.map(field=><label key={field.key}>{field.label}{field.type==='textarea'?<textarea required={field.required} value={String(form[field.key]??'')} onChange={e=>setForm({...form,[field.key]:e.target.value})}/>:field.type==='boolean'?<input type="checkbox" checked={Boolean(form[field.key])} onChange={e=>setForm({...form,[field.key]:e.target.checked})}/>:field.type==='status'?<select value={String(form[field.key]??'draft')} onChange={e=>setForm({...form,[field.key]:e.target.value})}><option value="draft">Draft</option><option value="published">Published</option><option value="approved">Approved</option><option value="pending">Pending</option><option value="archived">Archived</option></select>:field.type==='image'?<><input value={String(form[field.key]??'')} placeholder="Image URL" onChange={e=>setForm({...form,[field.key]:e.target.value})}/><input type="file" accept="image/*" onChange={async e=>{const file=e.target.files?.[0];if(file){setBusy(true);try{setForm({...form,[field.key]:await uploadAdminImage(file,section.collection)});}finally{setBusy(false);}}}}/></>:<input required={field.required} type={field.type==='number'?'number':'text'} value={String(form[field.key]??'')} onChange={e=>setForm({...form,[field.key]:field.type==='number'?Number(e.target.value):e.target.value})}/>}</label>)}<button className="cta-primary" disabled={busy}>{busy?'Saving…':'Save record'}</button></form></div>}</>}
