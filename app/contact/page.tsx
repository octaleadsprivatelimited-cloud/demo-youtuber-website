'use client';
import {useState} from 'react';import {PublicShell,useSiteSettings} from '@/components/SiteChrome';import {submitContact} from '@/services/public-content';
export default function Contact(){const settings=useSiteSettings();const [form,setForm]=useState({name:'',email:'',phone:'',message:''});const [status,setStatus]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setStatus('Sending…');try{await submitContact(form);setForm({name:'',email:'',phone:'',message:''});setStatus('Message received.');}catch(reason){setStatus(reason instanceof Error?reason.message:'Unable to send the message. Please try again.');}}return <PublicShell><main className="contact-page"><section className="page-hero"><p>CONTACT</p><h1>Talk to RJ Tractor Techs</h1><span>Send an editorial, business or general website enquiry.</span></section><section className="contact-grid"><article><h2>Start a conversation</h2><p>Contact details are managed through the website settings. You can also connect through the official YouTube channel.</p><p>{settings.email&&<a href={'mailto:'+settings.email}>{settings.email}</a>}</p><p>{settings.phone&&<a href={'tel:'+settings.phone}>{settings.phone}</a>}</p><a href={settings.youtube||'https://www.youtube.com/@Rjtractortechs'} target="_blank" rel="noreferrer">Visit RJ Tractor Techs on YouTube ↗</a></article><form onSubmit={submit}><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Message<textarea required value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label><button disabled={status==='Sending…'} className="cta-primary">Send message</button><small aria-live="polite">{status}</small></form></section></main></PublicShell>}
