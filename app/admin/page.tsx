'use client';
import {useEffect,useState} from 'react';
import {AdminShell} from '@/components/admin/AdminShell';
import {getAdminCounts} from '@/services/admin';

export default function AdminPage(){const [counts,setCounts]=useState<Record<string,number>>({});const [error,setError]=useState('');useEffect(()=>{getAdminCounts(['tractors','brands','leads','reviews','articles','dealers','users']).then(setCounts).catch(e=>setError(e instanceof Error?e.message:'Unable to load dashboard.'));},[]);return <AdminShell><header className="admin-heading"><div><p>OPERATIONS</p><h1>Dashboard</h1><span>Content, customers and marketplace activity in one workspace.</span></div><a className="cta-primary" href="/admin/tractors">Add tractor</a></header>{error&&<div className="admin-error">{error}</div>}<div className="admin-dashboard-grid">{Object.entries({tractors:'Tractors',brands:'Brands',leads:'Leads',reviews:'Reviews',articles:'Articles',dealers:'Dealers',users:'Users'}).map(([key,label])=><a href={key==='leads'?'/admin/leads':`/admin/${key}`} key={key}><span>{label}</span><strong>{counts[key]??'—'}</strong><small>Manage {label.toLowerCase()} →</small></a>)}</div><section className="admin-panel admin-quick"><h2>Publishing workflow</h2><p>Draft content remains visible only to administrators. Set a record to published when it is ready for the public website.</p><div><a href="/admin/articles">Create an article</a><a href="/admin/banners">Schedule a banner</a><a href="/admin/reviews">Moderate reviews</a><a href="/admin/seo">Update SEO</a></div></section></AdminShell>}
