import {BrandDetailPage} from '@/components/BrandDetailPage';import {titleFromSlug} from '@/utils/seo';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=titleFromSlug(slug);return{title:`${name} Tractors | RJ Tractor Techs`,description:`Explore ${name} tractor models, specifications, prices and reviews.`,alternates:{canonical:`/brand/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){return <BrandDetailPage slug={(await params).slug}/>}
