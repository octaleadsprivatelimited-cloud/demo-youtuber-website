import {CategoryPage} from '@/components/CategoryPage';import {titleFromSlug} from '@/utils/seo';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return{title:`${titleFromSlug(slug)} | RJ Tractor Techs`,alternates:{canonical:`/category/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){return <CategoryPage slug={(await params).slug}/>}
